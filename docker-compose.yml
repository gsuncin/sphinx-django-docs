# run one dir before
version: "3.7"
services:
  nginx:
    container_name: nginx-docs
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./docs/build/html:/usr/share/nginx/html # serve static
